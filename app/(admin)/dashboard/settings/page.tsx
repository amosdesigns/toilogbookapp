import React from 'react'

const SettingsPage = () => {
  // this page is for management of the settings in the admin dashboard
  // it should only be accessible to admin users or higher roles only. if you are not an admin user, you should be redirected to the dashboard page.
  //from this page, we can implement the logic to manage various settings related to the guard check in form. the list of items that a guard can check for, notification preferences, and other relevant configurations.

  return (
    <div>settings page</div>
  )
}

export default SettingsPage;
