import React from 'react'

const LocationManagePage = () => {
  // this page is for management of the locations in the admin dashboard
  // it display the location data in a table with options to add, edit, and delete locations -  it should only be accessible to admin users or higher roles only. if you are not an admin user, you should be redirected to the dashboard page.
  return <div>location page</div>;
};

export default LocationManagePage
